[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback – aplikace nemá server, veškeré routy obsluhuje index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
